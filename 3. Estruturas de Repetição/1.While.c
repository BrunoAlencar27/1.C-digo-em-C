#include <stdio.h>

int soma=0;

int main(){
    //Estruturada while
    while(soma<100){//Para quando soma for igual a 100.
        soma++;//Atribuição aritmética "++", incrementando 
               // mais 1 a variável soma.
    }
    printf("%d\n",soma);
    return 0;
}